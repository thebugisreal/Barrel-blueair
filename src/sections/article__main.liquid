<article class="article" itemscope itemtype="http://schema.org/BlogPosting" style="--max-width-desktop: {{ section.settings.max_width_desktop }};">

  <div class="article__header-container pt-lg mt-xs bg-gray-athans tabletl:mt-0 tabletl:pt-0 tabletl:flex">
    <header class="article__header mx-lg flex items-center tabletl:mx-lg tabletl:w-1/2">
      <div class="article__header-content mx-auto">
        <a href="{{ blog.url }}" class="btn pt-xs">
          <span class="btn__arrow">{% render 'icon-arrow-left' %}</span>
          <span class="btn__text">Back to {{ blog.title }}</span>
        </a>
        
        <h1 class="article__title font-900 my-md tabletl:my-lg tabletl:py-xs">{{ article.title }}</h1>
        <div class="article__author tabletl:flex tabletl:mb-md tabletl:gap-sm">
          <p class="p4 mb-md tabletl:mb-0">Reviewed by <span class="p3 underline">{{ article.author }}</span></p>
          <p class="hidden text-[#121A224D] tabletl:block">&bull;</p>
          <p class="p4">Updated {{ article.created_at | date: "%b %d, %Y"}}</p>
        </div>
      </div>

    </header>
  
    {% if article.image != blank %}
      <div class="article__image mt-lg pt-xs tabletl:mt-0 tabletl:pt-0 tabletl:w-1/2">{% render 'lazy-image' image: article.image img_classes:'h-full', picture_classes:' h-full object-cover' %}</div>
    {% endif %}
  </div>

  {% if article.content != blank %}
    <div class="article__content rte px-lg py-lg my-xs tabletl:my-xs tabletl:py-xxxl tabletl:px-0 tabletl:mx-auto tabletl:max-w-[80ch]">
      {{ article.content }}
    </div>
  {% endif %}


  {% comment %} Blog Comments {% endcomment %}
  {%- if blog.comments_enabled? and article.comments.size > 0-%}
    {%- assign anchorId = '#Comments-' | append: article.id -%}
    {% paginate article.comments by 5 %} 
      <div class="article__comments"> 
        <h2 class="article__comments-title">{{ article.comments_count }} Comments</h2>
        {% for comment in article.comments %}
          {% render 'article-comment' comment: comment %}
        {% endfor %}
        {% render 'pagination', paginate: paginate, anchor: anchorId %}
      </div>
    {% endpaginate %}

    <div class="article__new-comment-form">
      {% render 'new-comment-form', blog: blog %}
    </div>
  {%- endif -%}
  {% comment %} END Blog Comments {% endcomment %}

</article>


{% schema %}
{
  "name": "Article Header",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "max_width_desktop",
      "label": "Max Width of Text Content - Desktop",
      "info": "Make sure to include units",
      "default": "80ch"
    }
  ]
}
{% endschema %}
