<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="theme-color" content="">
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

    {% if settings.favicon != blank %}
      <link rel="shortcut icon" href="{{ settings.favicon | img_url: '32x32' }}" type="image/png">
    {% endif %}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'meta-tags' %}

    {%- comment -%} Redirect Account Addresses Page {%- endcomment -%}
    {% if template.name == 'addresses' %}
      <script> location.href = '{{ routes.account_url }}' </script>
    {% endif %}
  
    <script type="text/javascript" src="https://www.bugherd.com/sidebarv2.js?apikey=o1ztgcec0jdq8wblwfstya" async="true"></script>
    <!-- Affirm -->
    <script>
      _affirm_config = {
        public_api_key:  "0JDSO1NZ3ZHRQF0R",
        script:          "https://cdn1.affirm.com/js/v2/affirm.js"
      };
      (function(l,g,m,e,a,f,b){var d,c=l[m]||{},h=document.createElement(f),n=document.getElementsByTagName(f)[0],k=function(a,b,c){return function(){a[b]._.push([c,arguments])}};c[e]=k(c,e,"set");d=c[e];c[a]={};c[a]._=[];d._=[];c[a][b]=k(c,a,b);a=0;for(b="set add save post open empty reset on off trigger ready setProduct".split(" ");a<b.length;a++)d[b[a]]=k(c,e,b[a]);a=0;for(b=["get","token","url","items"];a<b.length;a++)d[b[a]]=function(){};h.async=!0;h.src=g[f];n.parentNode.insertBefore(h,n);delete g[f];d(g);l[m]=c})(window,_affirm_config,"affirm","checkout","ui","script","ready");
      // Use your live public API Key and https://cdn1.affirm.com/js/v2/affirm.js script to point to Affirm production environment.
    </script>
    <!-- End Affirm -->
    
    {{ content_for_header }}
  
    {{ 'vendor.css' | asset_url | stylesheet_tag }}
    {{ 'theme.css' | asset_url | stylesheet_tag }}

    <script>
      window.currency = {
        symbol: "{{ cart.currency.symbol }}",
        name: "{{ cart.currency.name }}",
        code: "{{ cart.currency.iso_code }}"
      };
    </script>

    <script>document.documentElement.className = document.documentElement.className.replace('no-js', 'js');</script>

    {% comment %} should be removed once klaviyo is integrated {% endcomment %}
    <script async type='text/javascript' src='https://static.klaviyo.com/onsite/js/klaviyo.js?company_id=WaDDdd'></script>
  </head>
  
  {%- capture body_classes -%}
    template-{{template.name}} {% if template.suffix %}template--{{template.suffix}}{% endif %}
  {%- endcapture -%}
  <body {% if template.name == 'login' %}id="recover"{% endif %} class="{{body_classes | strip}}">
    <a class="skip-to-content-link button visually-hidden" href="#MainContent">
      {{ "accessibility.skip_to_text" | t }}
    </a>

    {% sections 'header-group' %}
  
    {% render 'drawer--cart' %}
    {% render 'drawer--mobile-filter' %}
    {% render 'drawer--desktop-search' %}
    {% render 'modal--country-selector' %}

    {% if template.name == 'product' %}
      {% render 'product-sticky-bar' %}
    {% endif %}

    <main id="MainContent" class="main drawer-background modal-background grid {% if template.name == 'index' %}min-h-screen mt-[var(--header-height)]{% else %}min-h-header mt-[var(--header-height)]{% endif %}" role="main">
      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    <ul hidden>
      <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
    </ul>

    <script>
      window.routes = {
        cart_add_url: '{{ routes.cart_add_url }}',
        cart_change_url: '{{ routes.cart_change_url }}',
        cart_update_url: '{{ routes.cart_update_url }}',
        cart_url: '{{ routes.cart_url }}',
        predictive_search_url: '{{ routes.predictive_search_url }}',
      };
      window.PUB_SUB_EVENTS = {
        cartUpdate: 'cart-update',
        quantityUpdate: 'quantity-update',
        variantChange: 'variant-change',
        cartError: 'cart-error',
      };
    </script>

    <script src="{{ 'vendor.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'theme.js' | asset_url }}" defer="defer"></script>
  </body>
</html>
